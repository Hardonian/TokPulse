name: intel
on:
  schedule:
    - cron: "40 11 * * *"   # 07:40 Toronto (adjust for DST)
    - cron: "40 20 * * *"   # 16:40 Toronto
  workflow_dispatch: {}
jobs:
  intel:
    runs-on: ubuntu-latest
    permissions: { contents: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - run: |
          node tools/compute_kpis.mjs
          node tools/capcut_pack.mjs
          cd reports/intel/latest && zip -r intel-pack.zip . && cd -
          cd reports/creative && zip -r creative-pack.zip . && cd -
      - name: Upload daily creative pack to draft release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          prerelease: true
          files: |
            reports/intel/latest/intel-pack.zip
            reports/creative/creative-pack.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
